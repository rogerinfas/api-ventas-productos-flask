version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: products_db
    ports:
      - "4950:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
